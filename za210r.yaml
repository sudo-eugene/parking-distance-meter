substitutions:
  id: za210r # Generated a random hex - https://numbergenerator.org/hex-code-generator
  name: parking-distance-meter
  friendly_name: Parking Distance Meter v3
  update_interval: "0.1s"
  # GPIO Pin assignments
  echo_pin: GPIO5   # D1 on D1 Mini
  trigger_pin: GPIO4 # D2 on D1 Mini
  relay_pin: GPIO14   # D5 on D1 Mini
  dio_pin: GPIO12   # D6 on D1 Mini
  clk_pin: GPIO13   # D7 on D1 Mini
  
  # Sensor configuration
  max_distance: "2.0"           # Maximum valid distance in metres
  pulse_duration: "20us"
  
  # Display configuration
  display_update_interval: "0.10s"
  
  # Relay configuration
  debounce_time: "500"          # milliseconds
  static_timeout: "30000"      # 5 minutes in milliseconds
  static_diff: "10.0"            # cm difference to consider "static"

  # Unique Vars
  wifi_fallback_password: "3JNZEmBxckm0" # Set the wifi fallback password
  ota_password: "f53599c7983cbe5a3e0bfa9304975904" # This will be generated once you adopt the device into Home Assistant
  encryption_key: "ZGfikB+C0x6C97RzSMCogtFJc7oQ5+N6sW7ykVGk+lM=" # https://esphome.io/components/api.html#configuration-variables


esphome:
  name: ${name}-${id}
  friendly_name: ${friendly_name} ${id}
  min_version: 2024.6.0
  name_add_mac_suffix: false
  project:
    name: esphome.web
    version: '1.0'

esp8266:
  board: esp01_1m

# Enable logging
logger:

# Enable Home Assistant API
# Generate an encryption key at https://esphome.io/components/api.html#:~:text=to%206053.-,encryption,-(Optional)%3A%20Enable
api:
  encryption:
    key: ${encryption_key}

ota:
  - platform: esphome
    password: ${ota_password}

improv_serial:

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Parking Sensor ${id} Fallback"
    password: ${wifi_fallback_password}

captive_portal:

dashboard_import:
  package_import_url: github://esphome/example-configs/esphome-web/esp8266.yaml@main
  import_full_config: true

web_server:

sensor:
  - platform: ultrasonic
    trigger_pin: ${trigger_pin}
    echo_pin: ${echo_pin}
    name: "${friendly_name} Distance"
    update_interval: ${update_interval}
    pulse_time: ${pulse_duration}
    timeout: 4m  # Increased timeout to support measurements up to 4m (double the max_distance).
    filters:
      - median:
          window_size: 3
          send_every: 1
      - lambda: |-
          // Pass through all readings (in metres) converting to centimetres and round to whole numbers.
          // Add safety bias: respond immediately to approaching objects
          static float last_value = 999;
          if (isnan(x)) {
            return {};
          }
          
          float current_cm = round(x * 100.0);
          
          // If object is getting closer (distance decreasing), respond immediately
          // If object is moving away, use the rounded value for stability
          if (current_cm < last_value - 1) {
            // Object is getting closer - respond immediately
            last_value = current_cm;
            return current_cm;
          } else {
            // Moving away or stable - small smoothing for stability
            last_value = current_cm;
            return current_cm;
          }
    unit_of_measurement: "cm"
    id: distance_sensor

switch:
  - platform: gpio
    pin: ${relay_pin}
    id: parking_sensor_relay
    name: "${friendly_name} Relay Switch"
    inverted: true
    restore_mode: ALWAYS_ON

display:
  - platform: tm1637
    id: led_display
    clk_pin: ${clk_pin}
    dio_pin: ${dio_pin}
    update_interval: ${display_update_interval}
    lambda: |-
      auto distance = id(distance_sensor).state;
      if (!isnan(distance)) {
        // Display the integer value only
        it.printf("%4.0f", distance);
      }

interval:
  - interval: 150ms
    then:
      - lambda: |-
          // ------------------------------------------------
          // --- CONFIGURABLE PARAMETERS
          // ------------------------------------------------
          const float DISTANCE_THRESHOLD_CM = ${max_distance} * 100.0f;
          const unsigned long DEBOUNCE_MS = ${debounce_time};
          const unsigned long STATIC_TIMEOUT_MS = ${static_timeout};
          const float STATIC_DIFF_CM = ${static_diff};

          // Relay logic: Relay ON = display OFF, Relay OFF = display ON.
          static bool relay_state = false;
          static unsigned long last_change_time = 0;
          static float stable_baseline = NAN;
          static unsigned long stable_since = 0;
          static bool in_static_timeout = false;  // Track if we're in static timeout mode

          float distance = id(distance_sensor).state;
          if (isnan(distance)) {
            return;
          }

          // ------------------------------------------------------------------
          // 1) Immediately force relay off if measured distance exceeds max_distance.
          // ------------------------------------------------------------------
          if (distance > DISTANCE_THRESHOLD_CM) {
            // Turn display OFF when distance > max_distance
            id(parking_sensor_relay).turn_off();
            relay_state = false;
            last_change_time = millis();
            in_static_timeout = false;  // Reset static timeout when out of range
            return;
          } 
          
          // ------------------------------------------------
          // 2) Update "static" detection for distances ≤ max_distance.
          // ------------------------------------------------
          float diff = fabs(distance - stable_baseline);
          if (isnan(stable_baseline) || diff > STATIC_DIFF_CM) {
            stable_baseline = distance;
            stable_since = millis();
            in_static_timeout = false;  // Reset static timeout when movement detected
          }

          // ------------------------------------------------
          // 3) Determine relay state based on static timeout
          // ------------------------------------------------
          bool stable_for_timeout = (millis() - stable_since >= STATIC_TIMEOUT_MS);
          
          if (stable_for_timeout && !in_static_timeout) {
            // Just reached static timeout - turn display off
            id(parking_sensor_relay).turn_off();
            relay_state = false;
            in_static_timeout = true;
          } else if (!stable_for_timeout && !in_static_timeout) {
            // Normal operation - display should be on when within range
            id(parking_sensor_relay).turn_on();
            relay_state = true;
          }
          // Stay in current state if in static timeout until movement is detected
